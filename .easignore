# Large asset dumps — never needed in the EAS build archive
attached_assets/

# Other artifacts and server code — EAS only needs ags-mobile
artifacts/ags-fretboard/
artifacts/ags-intro-video/
artifacts/api-server/
artifacts/mockup-sandbox/

# Dev/agent state
.local/
.agents/

# Node modules (EAS installs fresh)
node_modules/
**/node_modules/

# Build outputs
**/dist/
**/.next/
